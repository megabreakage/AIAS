$kind: collection
description: |-
  Central continent management endpoints (super-admin only).

  All endpoints require super-admin Bearer token (central_token).

  Create Continent auto-saves continent_identifier for subsequent requests.
order: 4000
