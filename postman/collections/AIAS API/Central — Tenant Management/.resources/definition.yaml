$kind: collection
description: |-
  Tenant CRUD on the central domain. All endpoints require super-admin Bearer token (sa_token).

  Create Tenant auto-saves tenant_id for use in tenant-domain requests.
order: 3000
