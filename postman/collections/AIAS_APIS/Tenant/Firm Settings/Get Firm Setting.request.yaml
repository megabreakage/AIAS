$kind: http-request
name: Get Firm Setting
description: 'Retrieve a single firm setting by its `identifier` string.


  Replace `{{firm_settings_id}}` with the identifier saved after creation.'
url: '{{tenant_base_url}}/v1/firm-settings/{{firm_settings_id}}'
method: GET
headers:
  Accept: application/json
  Content-Type: application/json
queryParams:
- key: tenant_id
  value: '{{tenant_id}}'
  description: Tenant identifier string (required)
scripts:
- type: afterResponse
  code: |-
    pm.test('Response is JSON', () => { pm.response.to.be.json; });
    pm.test('Status is success', () => {
        const j = pm.response.json();
        pm.expect(j).to.have.property('status', 'success');
    });
  language: text/javascript
order: 3000
