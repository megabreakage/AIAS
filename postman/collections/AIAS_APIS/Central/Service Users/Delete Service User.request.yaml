$kind: http-request
name: Delete Service User
description: Soft-delete a service user record. The record is not permanently removed.
  Restore it with the corresponding `POST /service-users/{{id_id}}/restore` request.
url: '{{base_url}}/v1/service-users/{{id_id}}'
method: DELETE
headers:
  Accept: application/json
  Content-Type: application/json
scripts:
- type: afterResponse
  code: |-
    pm.test('Response is JSON', () => { pm.response.to.be.json; });
    pm.test('Status is success', () => {
        const j = pm.response.json();
        pm.expect(j).to.have.property('status', 'success');
    });
  language: text/javascript
order: 5000
