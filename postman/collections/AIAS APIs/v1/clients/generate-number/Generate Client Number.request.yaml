$kind: http-request
description: Generate Client Number
url: "{{baseUrl}}/v1/clients/generate-number?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
body:
  type: text
  content: ""
order: 1000
