$kind: http-request
description: Soft-delete a client record. The record is not permanently removed. Restore it with the corresponding `POST /clients/{{client_id}}/restore` request.
url: "{{baseUrl}}/v1/clients/:client_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: client_id
    value: "{{client_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
