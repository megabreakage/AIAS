$kind: http-request
description: Restore Client
url: "{{baseUrl}}/v1/clients/:client_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: client_id
    value: "{{client_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
