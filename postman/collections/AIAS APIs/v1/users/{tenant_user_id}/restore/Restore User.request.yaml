$kind: http-request
description: Restore a soft-deleted tenant user account.
url: "{{baseUrl}}/v1/users/:tenant_user_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: tenant_user_id
    value: "{{tenant_user_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
