$kind: http-request
description: Soft-delete a user record. The record is not permanently removed. Restore it with the corresponding `POST /users/{{user_id}}/restore` request.
url: "{{baseUrl}}/v1/users/:user_id"
method: DELETE
pathVariables:
  - key: user_id
    value: "{{user_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
