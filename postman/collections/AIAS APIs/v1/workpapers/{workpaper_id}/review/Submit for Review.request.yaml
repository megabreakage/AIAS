$kind: http-request
description: Submit for Review
url: "{{baseUrl}}/v1/workpapers/:workpaper_id/review"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: workpaper_id
    value: "{{workpaper_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
