$kind: http-request
description: Soft-delete a preamble record. The record is not permanently removed. Restore it with the corresponding `POST /preambles/{{preamble_id}}/restore` request.
url: "{{baseUrl}}/v1/preambles/:preamble_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: preamble_id
    value: "{{preamble_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
