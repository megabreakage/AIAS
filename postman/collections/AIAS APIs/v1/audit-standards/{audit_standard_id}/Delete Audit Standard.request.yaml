$kind: http-request
description: Soft-delete a audit standard record. The record is not permanently removed. Restore it with the corresponding `POST /audit-standards/{{audit_standard_id}}/restore` request.
url: "{{baseUrl}}/v1/audit-standards/:audit_standard_id"
method: DELETE
pathVariables:
  - key: audit_standard_id
    value: "{{audit_standard_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
