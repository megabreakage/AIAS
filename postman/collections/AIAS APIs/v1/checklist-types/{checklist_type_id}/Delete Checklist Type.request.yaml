$kind: http-request
description: Soft-delete a checklist type record. The record is not permanently removed. Restore it with the corresponding `POST /checklist-types/{{checklist_type_id}}/restore` request.
url: "{{baseUrl}}/v1/checklist-types/:checklist_type_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: checklist_type_id
    value: "{{checklist_type_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
