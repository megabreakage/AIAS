$kind: http-request
description: |-
  Retrieve a single checklist type by its `identifier` string.

  Replace `{{checklist_type_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/checklist-types/:checklist_type_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: checklist_type_id
    value: "{{checklist_type_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
