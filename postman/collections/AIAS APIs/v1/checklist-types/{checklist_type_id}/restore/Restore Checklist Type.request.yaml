$kind: http-request
description: Restore a soft-deleted checklist type record.
url: "{{baseUrl}}/v1/checklist-types/:checklist_type_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: checklist_type_id
    value: "{{checklist_type_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
