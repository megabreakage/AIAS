$kind: http-request
description: |-
  Retrieve a single audit template by its `identifier` string.

  Replace `{{audit_template_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/audit-templates/:audit_template_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: audit_template_id
    value: "{{audit_template_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
