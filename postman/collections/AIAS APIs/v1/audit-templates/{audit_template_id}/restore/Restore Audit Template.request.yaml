$kind: http-request
description: Restore Audit Template
url: "{{baseUrl}}/v1/audit-templates/:audit_template_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: audit_template_id
    value: "{{audit_template_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
