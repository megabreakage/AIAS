$kind: http-request
description: Soft-delete a firm setting record. The record is not permanently removed. Restore it with the corresponding `POST /firm-settings/{{firm_settings_id}}/restore` request.
url: "{{baseUrl}}/v1/firm-settings/:firm_settings_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: firm_settings_id
    value: "{{firm_settings_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
