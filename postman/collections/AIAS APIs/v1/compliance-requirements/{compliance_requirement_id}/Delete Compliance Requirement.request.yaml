$kind: http-request
description: Soft-delete a compliance requirement record. The record is not permanently removed. Restore it with the corresponding `POST /compliance-requirements/{{compliance_requirement_id}}/restore` request.
url: "{{baseUrl}}/v1/compliance-requirements/:compliance_requirement_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: compliance_requirement_id
    value: "{{compliance_requirement_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
