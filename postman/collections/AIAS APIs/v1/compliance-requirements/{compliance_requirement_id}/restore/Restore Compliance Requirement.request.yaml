$kind: http-request
description: Restore Compliance Requirement
url: "{{baseUrl}}/v1/compliance-requirements/:compliance_requirement_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: compliance_requirement_id
    value: "{{compliance_requirement_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
