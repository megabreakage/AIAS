$kind: http-request
description: Compliance Summary
url: "{{baseUrl}}/v1/compliance-requirements/summary?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
body:
  type: text
  content: ""
order: 1000
