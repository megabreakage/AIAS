$kind: http-request
description: Verify Evidence
url: "{{baseUrl}}/v1/compliance-evidence/:compliance_evidence_id/verify"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: compliance_evidence_id
    value: "{{compliance_evidence_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
