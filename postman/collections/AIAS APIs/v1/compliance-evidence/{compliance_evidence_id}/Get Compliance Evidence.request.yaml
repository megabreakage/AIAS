$kind: http-request
description: |-
  Retrieve a single compliance evidence by its `identifier` string.

  Replace `{{compliance_evidence_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/compliance-evidence/:compliance_evidence_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: compliance_evidence_id
    value: "{{compliance_evidence_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
