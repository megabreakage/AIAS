$kind: http-request
description: Soft-delete a compliance evidence record. The record is not permanently removed. Restore it with the corresponding `POST /compliance-evidence/{{compliance_evidence_id}}/restore` request.
url: "{{baseUrl}}/v1/compliance-evidence/:compliance_evidence_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: compliance_evidence_id
    value: "{{compliance_evidence_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
