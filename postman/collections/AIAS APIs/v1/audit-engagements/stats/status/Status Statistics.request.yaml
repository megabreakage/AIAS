$kind: http-request
description: Status Statistics
url: "{{baseUrl}}/v1/audit-engagements/stats/status?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
body:
  type: text
  content: ""
order: 1000
