$kind: http-request
description: Approve Engagement
url: "{{baseUrl}}/v1/audit-engagements/:audit_engagement_id/approve"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: audit_engagement_id
    value: "{{audit_engagement_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
