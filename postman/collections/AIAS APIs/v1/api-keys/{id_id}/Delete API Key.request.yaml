$kind: http-request
description: Soft-delete a api key record. The record is not permanently removed. Restore it with the corresponding `POST /api-keys/{{id_id}}/restore` request.
url: "{{baseUrl}}/v1/api-keys/:id_id"
method: DELETE
pathVariables:
  - key: id_id
    value: "{{id_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
