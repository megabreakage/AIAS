$kind: http-request
description: Restore a soft-deleted audit record.
url: "{{baseUrl}}/v1/audits/:audit_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: audit_id
    value: "{{audit_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
