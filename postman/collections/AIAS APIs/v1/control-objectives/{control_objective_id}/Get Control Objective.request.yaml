$kind: http-request
description: |-
  Retrieve a single control objective by its `identifier` string.

  Replace `{{control_objective_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/control-objectives/:control_objective_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: control_objective_id
    value: "{{control_objective_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
