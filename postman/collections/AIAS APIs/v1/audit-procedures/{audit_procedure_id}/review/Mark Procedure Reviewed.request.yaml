$kind: http-request
description: Mark Procedure Reviewed
url: "{{baseUrl}}/v1/audit-procedures/:audit_procedure_id/review"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: audit_procedure_id
    value: "{{audit_procedure_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
