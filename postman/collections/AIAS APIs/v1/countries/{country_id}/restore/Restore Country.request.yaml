$kind: http-request
description: Restore a soft-deleted country record.
url: "{{baseUrl}}/v1/countries/:country_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: country_id
    value: "{{country_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
