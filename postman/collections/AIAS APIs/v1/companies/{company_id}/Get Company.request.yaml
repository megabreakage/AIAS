$kind: http-request
description: |-
  Retrieve a single company by its `identifier` string.

  Replace `{{company_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/companies/:company_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: company_id
    value: "{{company_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
