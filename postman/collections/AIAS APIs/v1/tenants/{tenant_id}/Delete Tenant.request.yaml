$kind: http-request
description: Soft-delete a tenant record. The record is not permanently removed. Restore it with the corresponding `POST /tenants/{{tenant_id}}/restore` request.
url: "{{baseUrl}}/v1/tenants/:tenant_id"
method: DELETE
pathVariables:
  - key: tenant_id
    value: "{{tenant_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
