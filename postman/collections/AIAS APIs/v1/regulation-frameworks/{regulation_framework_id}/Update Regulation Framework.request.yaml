$kind: http-request
description: |-
  Update an existing regulation framework record.

  Send only the fields you want to change — all fields are optional on update. Replace `{{regulation_framework_id}}` with the identifier of the record to update.
url: "{{baseUrl}}/v1/regulation-frameworks/:regulation_framework_id"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  - key: regulation_framework_id
    value: "{{regulation_framework_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "name": "SOX",
      "code": "SOX",
      "description": "Sarbanes-Oxley Act — US public company accounting reform",
      "jurisdiction": "United States",
      "status": true
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "code": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "jurisdiction": {
          "type": "string"
        },
        "status": {
          "type": "boolean"
        }
      }
    }
order: 2000
