$kind: http-request
description: Restore Regulation Framework
url: "{{baseUrl}}/v1/regulation-frameworks/:regulation_framework_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: regulation_framework_id
    value: "{{regulation_framework_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
