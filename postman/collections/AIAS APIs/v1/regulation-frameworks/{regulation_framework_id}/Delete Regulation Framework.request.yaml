$kind: http-request
description: Soft-delete a regulation framework record. The record is not permanently removed. Restore it with the corresponding `POST /regulation-frameworks/{{regulation_framework_id}}/restore` request.
url: "{{baseUrl}}/v1/regulation-frameworks/:regulation_framework_id"
method: DELETE
pathVariables:
  - key: regulation_framework_id
    value: "{{regulation_framework_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
