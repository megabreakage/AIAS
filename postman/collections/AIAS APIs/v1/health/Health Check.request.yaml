$kind: http-request
description: |-
  Verify the API server is running and reachable.

  No authentication required. Returns `200 OK` with a status message when the server is healthy. Both central (`{{baseUrl}}/v1/health`) and tenant (`{{baseUrl}}/v1/health`) health endpoints are available.
url: "{{baseUrl}}/v1/health"
method: GET
body:
  type: text
  content: ""
order: 1000
