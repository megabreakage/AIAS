$kind: http-request
description: Setup MFA
url: "{{baseUrl}}/v1/mfa/setup"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
