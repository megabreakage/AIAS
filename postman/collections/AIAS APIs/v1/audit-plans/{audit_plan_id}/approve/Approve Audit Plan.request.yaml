$kind: http-request
description: Approve Audit Plan
url: "{{baseUrl}}/v1/audit-plans/:audit_plan_id/approve"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: audit_plan_id
    value: "{{audit_plan_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
