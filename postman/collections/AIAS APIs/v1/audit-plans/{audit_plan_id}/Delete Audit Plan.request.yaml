$kind: http-request
description: Soft-delete a audit plan record. The record is not permanently removed. Restore it with the corresponding `POST /audit-plans/{{audit_plan_id}}/restore` request.
url: "{{baseUrl}}/v1/audit-plans/:audit_plan_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: audit_plan_id
    value: "{{audit_plan_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
