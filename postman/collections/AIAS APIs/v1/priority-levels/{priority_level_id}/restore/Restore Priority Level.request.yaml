$kind: http-request
description: Restore a soft-deleted priority level record.
url: "{{baseUrl}}/v1/priority-levels/:priority_level_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: priority_level_id
    value: "{{priority_level_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
