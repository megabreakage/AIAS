$kind: http-request
description: Get Service User API Keys
url: "{{baseUrl}}/v1/service-users/:id_id/api-keys"
method: GET
pathVariables:
  - key: id_id
    value: "{{id_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
