$kind: http-request
description: Activate Service User
url: "{{baseUrl}}/v1/service-users/:id_id/activate"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: id_id
    value: "{{id_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
