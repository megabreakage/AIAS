$kind: http-request
description: Restore Risk Category
url: "{{baseUrl}}/v1/risk-categories/:risk_category_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: risk_category_id
    value: "{{risk_category_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
