$kind: http-request
description: Restore a soft-deleted continent record.
url: "{{baseUrl}}/v1/continents/:continent_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: continent_id
    value: "{{continent_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object"
    }
order: 1000
