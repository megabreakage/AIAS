$kind: http-request
description: Reject Response
url: "{{baseUrl}}/v1/finding-responses/:finding_response_id/reject"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: finding_response_id
    value: "{{finding_response_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
