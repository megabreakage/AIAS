$kind: http-request
description: |-
  Retrieve a single finding respons by its `identifier` string.

  Replace `{{finding_response_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/finding-responses/:finding_response_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: finding_response_id
    value: "{{finding_response_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
