$kind: http-request
description: Soft-delete a finding respons record. The record is not permanently removed. Restore it with the corresponding `POST /finding-responses/{{finding_response_id}}/restore` request.
url: "{{baseUrl}}/v1/finding-responses/:finding_response_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: finding_response_id
    value: "{{finding_response_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
