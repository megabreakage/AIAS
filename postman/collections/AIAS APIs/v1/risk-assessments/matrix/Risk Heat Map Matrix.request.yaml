$kind: http-request
description: Risk Heat Map Matrix
url: "{{baseUrl}}/v1/risk-assessments/matrix?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
body:
  type: text
  content: ""
order: 1000
