$kind: http-request
description: |-
  Retrieve a single risk assessment by its `identifier` string.

  Replace `{{risk_assessment_id}}` with the identifier saved after creation.
url: "{{baseUrl}}/v1/risk-assessments/:risk_assessment_id?tenant_id={{tenant_id}}"
method: GET
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: risk_assessment_id
    value: "{{risk_assessment_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 1000
