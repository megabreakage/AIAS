$kind: http-request
description: Approve Risk Assessment
url: "{{baseUrl}}/v1/risk-assessments/:risk_assessment_id/approve"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: risk_assessment_id
    value: "{{risk_assessment_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
