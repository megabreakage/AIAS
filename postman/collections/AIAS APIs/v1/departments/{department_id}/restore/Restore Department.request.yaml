$kind: http-request
description: Restore a soft-deleted department record.
url: "{{baseUrl}}/v1/departments/:department_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: department_id
    value: "{{department_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
