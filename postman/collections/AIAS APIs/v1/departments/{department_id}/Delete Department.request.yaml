$kind: http-request
description: Soft-delete a department record. The record is not permanently removed. Restore it with the corresponding `POST /departments/{{department_id}}/restore` request.
url: "{{baseUrl}}/v1/departments/:department_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: department_id
    value: "{{department_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
