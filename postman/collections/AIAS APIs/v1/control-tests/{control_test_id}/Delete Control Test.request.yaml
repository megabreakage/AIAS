$kind: http-request
description: Soft-delete a control test record. The record is not permanently removed. Restore it with the corresponding `POST /control-tests/{{control_test_id}}/restore` request.
url: "{{baseUrl}}/v1/control-tests/:control_test_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: control_test_id
    value: "{{control_test_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
