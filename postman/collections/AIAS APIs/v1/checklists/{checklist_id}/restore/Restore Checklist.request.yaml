$kind: http-request
description: Restore a soft-deleted checklist record.
url: "{{baseUrl}}/v1/checklists/:checklist_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: checklist_id
    value: "{{checklist_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
