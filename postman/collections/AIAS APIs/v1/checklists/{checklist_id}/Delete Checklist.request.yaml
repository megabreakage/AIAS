$kind: http-request
description: Soft-delete a checklist record. The record is not permanently removed. Restore it with the corresponding `POST /checklists/{{checklist_id}}/restore` request.
url: "{{baseUrl}}/v1/checklists/:checklist_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: checklist_id
    value: "{{checklist_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
