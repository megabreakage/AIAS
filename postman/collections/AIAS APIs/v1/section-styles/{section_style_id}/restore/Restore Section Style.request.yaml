$kind: http-request
description: Restore a soft-deleted section style record.
url: "{{baseUrl}}/v1/section-styles/:section_style_id/restore"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: section_style_id
    value: "{{section_style_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
