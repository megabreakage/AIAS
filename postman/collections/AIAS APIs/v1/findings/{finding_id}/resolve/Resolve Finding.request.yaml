$kind: http-request
description: Resolve Finding
url: "{{baseUrl}}/v1/findings/:finding_id/resolve"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: finding_id
    value: "{{finding_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "tenant_id": "{{tenant_id}}"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "tenant_id": {
          "type": "string"
        }
      }
    }
order: 1000
