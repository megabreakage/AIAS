$kind: http-request
description: Soft-delete a finding record. The record is not permanently removed. Restore it with the corresponding `POST /findings/{{finding_id}}/restore` request.
url: "{{baseUrl}}/v1/findings/:finding_id?tenant_id={{tenant_id}}"
method: DELETE
queryParams:
  - key: tenant_id
    value: "{{tenant_id}}"
    description: Tenant identifier string (required)
    $type: string
pathVariables:
  - key: finding_id
    value: "{{finding_id}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
order: 3000
