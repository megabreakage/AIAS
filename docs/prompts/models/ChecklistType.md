# AI Prompts for ChecklistType Module

## 1. ChecklistType Module

/caveman Create a completly featured tenant-scoped **ChecklistType** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: string, indexed, tenants.identifier from the Tenants module (No FK constraint),
- `name`: string, not null,
- `description`: text, nullable,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable, No FK constraint),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable, No FK constraint),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

## 2. ChecklistType Module

/caveman Create a completly featured tenant-scoped **ChecklistType** module. Refer to `docs/prompts/models/ChecklistType.md` for the table structure and functionalities.
