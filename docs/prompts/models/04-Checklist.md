# AI Prompts for ChecklistSection Module

## 1. Checklist Module

/caveman Create a completly featured tenant-scoped **Checklist** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: string, indexed,
- `reference_number`: string, unique, indexed, formatted as "CL-{preamble_id}-{timestamp}",}" (Create a function in the Model to generate this format),
- `name`: string, not null,
- `quality_controller_id`: unsignedBigInteger, (user id of the assigned quality controller, nullable, No FK constraint),
- `preamble_id`: foreignId, references `id` on `preambles` table, onDelete null, nullable,
- `checklist_type_id`: foreignId, references `id` on `checklist_types` table, onDelete null, nullable,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

Create comprehensive Unit & Feature Test for **ChecklistSectionStyle** module covering, its model, migration, repository, service(if exists), controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **ChecklistSectionStyle** module, and ensure that all endpoints are properly documented with request and response examples.

## 2. Relationships

Checklist has a one-to-many relationship with SectionStyles. Create a completly featured tenant-scoped **ChecklistSectionStyle** module. Refer to `docs/prompts/models/ChecklistSectionStyle.md` for Model specifications.

Create comprehensive Unit & Feature Test for **ChecklistSectionStyle** module covering, its model, migration, repository, service(if exists), controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **ChecklistSectionStyle** module, and ensure that all endpoints are properly documented with request and response examples.
