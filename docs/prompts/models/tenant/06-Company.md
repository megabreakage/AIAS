# AI Prompts for Company Module

## 1. Company Module

Create a completly featured *Tenant-Scoped* **Company** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: required, string, indexed,
- `reference_number`: string, unique, indexed, formatted as "CL-{preamble_id}-{timestamp}",}" (Create a function in the Model to generate this format),
- `name`: required, string, not null,
- `address`: text, nullable,
- `office_location`: string, nullable,
- `latitude`: decimal(10, 7), nullable,(automatically filled based on the office_location using a geocoding service via google maps API, create a function in the Model to handle this),
- `longitude`: decimal(10, 7), nullable(automatically filled based on the office_location using a geocoding service via google maps API, create a function in the Model to handle this),
- `postal_code`: string, nullable,
- `country_id`: unsignedBigInteger, (`id` of `country` from countries table, No FK constraint) nullable but required,(automatically filled based on the office_location using a geocoding service via google maps API, create a function in the Model to handle this - Searches the country based on the geocoding result and fills the country_id. If the officce_location is not found or provided or the geocoding fails, the country_id will be null and should be selected manually by the user),
- `level_of_operations`: enum('local', 'regional', 'international'), default 'local',
- `trading_name`: string, nullable,
- `website`: string, nullable,
- `email`: string, nullable,
- `phone`: string, nullable,
- `logo`: string, nullable (Upload the logo image and store the path in this column, create a function in the Model to handle the file upload and storage),
- `description`: text, nullable,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

Create comprehensive *Unit & Feature Tests* for **Company** module covering, its model, migration, repository, service(if exists), policy, controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **Company** module, and ensure that all endpoints are properly documented with request and response examples.

```python3 docs/prompts/aias/scripts/generate-module.py \\
    --module-name=Company \\
    --table-name=companies \\
    --columns="id:primary,identifier:uuid,tenant_id:string,reference_number:string,name:string,address:text,office_location:string,latitude:decimal(10,7),longitude:decimal(10,7),postal_code:string,country_id:unsignedBigInteger,level_of_operations:enum('local', 'regional', 'international'),trading_name:string,website:string,email:string,phone:string,logo:string,description:text,is_active:boolean,is_featured:boolean,created_by:unsignedBigInteger,updated_by:unsignedBigInteger,created_at:timestamp,updated_at:timestamp,deleted_at:timestamp" \
    --relationships="company_contacts:hasMany" \
    --tenant-scoped=true
```

## 2. Relationships

Company Module has a `hasMany` relationship with `company_contacts`. Create `company_contacts` table with the following columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `company_id`: foreign key referencing `id` in `companies` table, on delete cascade,
- `user_id`: unsignedBigInteger, (user id of the contact person, nullable),
- `contact_type`: enum('primary', 'secondary', 'billing', 'technical'), default 'primary',
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

While creating or updating a Company, `company_contacts`(*tenant-scoped*) can also be passed via the request to be created or updated. Implement the necessary logic in the controller and service layer to handle the creation and updating of company_contacts when a company is created or updated.

Create comprehensive *Unit & Feature Tests* for Company Contacts covering, the ability to create company and passing company_contacts in the request, and ensuring that the company_contacts are properly created and associated with the company. Also, test the updating of company_contacts when a company is updated, and ensure that the changes are properly reflected in the database. Update Postman collection with endpoints for creating and updating companies with company_contacts, and ensure that all endpoints are properly documented with request and response examples.
