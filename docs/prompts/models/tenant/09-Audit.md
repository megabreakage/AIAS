# AI Prompts for Audit Module

## 1. Audit Module

Create a completly featured *Tenant-Scoped* **Audit** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: required, string, indexed,
- `reference_number`: string, nullable, unique (generate reference number in the creating event, format: AUD-{Year}{audit_id}-{time})
- `name`: required, string, not null,
- `checklist_id`: foreignId constraint to `checklists` table, nullable (assuming an audit can be created without a checklist, but can be associated later), on delete set null,
- `task_type_id`: foreignId constraint to `task_types` table, nullable (assuming an audit can be created without a task type, but can be associated later), on delete set null,
- `scope`: enum('internal', 'external', 'service_provider', 'supplier'), default 'internal',
- `department_id`: foreignId constraint to `departments` table, nullable (assuming an audit can be created without a department, but can be associated later), on delete set null,
- `audit_start_date`: timestamp, required,
- `audit_end_date`: timestamp, nullable (can be set when the audit is completed),
- `lead_auditor_id`: unsignedBigInteger, (user id of the lead auditor, nullable),
- `quality_manager_id`: unsignedBigInteger, (user id of the quality manager overseeing the audit, nullable),
- `add_appendix`: boolean, default false, (indicates if the audit has an appendix attached),
- `description`: text, nullable,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

Create comprehensive *Unit & Feature Tests* for **Audit** module covering, its model, migration, repository, service(if exists), policy, controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **Audit** module, and ensure that all endpoints are properly documented with request and response examples.

```python3 docs/prompts/aias/scripts/generate-module.py \\
    --model-name=Audit \\
    --table-name=audits \\
    --columns="id:primary,identifier:uuid,tenant_id:string,reference_number:string,name:string,checklist_id:foreignId,task_type_id:foreignId,scope:enum('internal','external','service_provider','supplier'),department_id:foreignId,audit_start_date:timestamp,audit_end_date:timestamp,lead_auditor_id:unsignedBigInteger,quality_manager_id:unsignedBigInteger,add_appendix:boolean,description:text,is_active:boolean,is_featured:boolean,created_by:unsignedBigInteger,updated_by:unsignedBigInteger,created_at:timestamp,updated_at:timestamp,deleted_at:timestamp" \
    --relationships="" \
    --tenant-scoped=true
```

## 2. Relationships

Audit Module has a `hasMany` relationship with `audit_status_stages`. Create `audit_status_stages` table with the following columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `audit_id`: foreignId constraint to `audits` table, on delete cascade,
- `status`: enum('scheduled', 'in_progress', 'completed', 'closed'), default 'scheduled' (Create ENUMS as constants in the AuditStatusStage Model),
- `changed_at`: timestamp, (time when the status was changed),
- `changed_by`: unsignedBigInteger, (user id of the person who changed the status, nullable),
- `notes`: text, nullable (any notes related to the status change),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

When an Audit is created, an initial entry should be created in the `audit_status_stages` table with the status set to 'scheduled'. Implement the necessary logic in the controller and service layer to handle the creation of this initial status stage when a new Audit is created.

Create comprehensive *Unit & Feature Tests* for **AuditStatusStage** module covering, its model, migration, repository, service(if exists), policy, controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **AuditStatusStage** module, and ensure that all endpoints are properly documented with request and response examples.
