# AI Prompts for PriorityLevel Module

## 1. PriorityLevel Module

Create a completly featured *Tenant-Scoped* **PriorityLevel** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: required, string, indexed,
- `name`: required, string, not null,
- `colour_code`: string, nullable,
- `period`: integer, nullable, (number of days for the priority level)
- `period_unit`: enum('D', 'W', 'M', 'Y'), nullable, (D for Day, W for Week, M for Month, Y for Year)
- `description`: text, nullable,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

Create comprehensive *Unit & Feature Tests* for **PriorityLevel** module covering, its model, migration, repository, service(if exists), policy, controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **PriorityLevel** module, and ensure that all endpoints are properly documented with request and response examples.

```python3 docs/prompts/aias/scripts/generate-module.py \\
    --model-name=PriorityLevel \\
    --table-name=priority_levels \\
    --columns="id:primary,identifier:uuid,tenant_id:string,name:string,colour_code:string,period:integer,period_unit:enum('D','W','M','Y'),description:text,is_active:boolean,is_featured:boolean,created_by:unsignedBigInteger,updated_by:unsignedBigInteger,created_at:timestamp,updated_at:timestamp,deleted_at:timestamp" \
    --relationships="" \
    --tenant-scoped=true
```
