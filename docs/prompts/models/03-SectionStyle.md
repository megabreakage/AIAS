# AI Prompts for SectionStyle Module

## 1. SectionStyle Module

/caveman Create a completly featured tenant-scoped **SectionStyle** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: required, string, indexed, tenants.identifier from the Tenants module (No FK constraint),
- `name`: required, string, not null,
- `description`: text, nullable,
- `columns`: integer, default 1,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable, No FK constraint),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable, No FK constraint),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)

Create comprehensive Unit & Feature Test for **SectionStyle** module covering, its model, migration, repository, service(if exists), controller, requests & validations, and API resource & collection. Update Postman collection with endpoints for **SectionStyle** module, and ensure that all endpoints are properly documented with request and response examples.
