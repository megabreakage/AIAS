# AI Prompts for ChecklistSectionStyle Module

## 1. ChecklistSectionStyle Module

/caveman Create a completly featured tenant-scoped **ChecklistSection** module with the following table columns:

- `id`: primary key, auto-increaments,(not fillable, generated by the database)
- `identifier`: uuid, unique, indexed,not null (not fillable on Model, generated in the creating event: Defined in the BaseModel's creating event)
- `tenant_id`: string, indexed,
- `reference_number`: string, not null, formatted as "CSS-{checklist_id}-{timestamp}",}" (Create a function in the Model to generate this format),
- `name`: string, not null,
- `checklist_id`: foreign key referencing `id` on `checklists` table, indexed, not null,
- `section_style_id`: foreign key referencing `id` on `section_styles` table, indexed, not null,
- `description`: text, nullable,
- `is_active`: boolean, default true,
- `is_featured`: boolean, default false,
- `created_by`: unsignedBigInteger, (user id of the creator, nullable),
- `updated_by`: unsignedBigInteger, (user id of the last updater, nullable),
- `created_at`: timestamp,
- `updated_at`: timestamp,
- `deleted_at`: timestamp, nullable (for soft deletes)
